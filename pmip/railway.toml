[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npm run build && npx prisma migrate deploy && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
